# Plan/spec — 设计规格与跨仓契约

本目录存放**现行有效**的 spec / design / backend handoff：

- 已完成并消费的旧文档归档至 `Plan/Archived/`（历史保留，不再作为施工依据）；
- 明确暂停、暂缓的放入 `Plan/待续/`；
- 仍在本目录中的文档视为 active reference，修改或废弃前需先确认引用方。

与后端 `ExoCore/Plan/`（Archived / 待续 / design / 需求文档）对齐；前端暂以本目录 + `Plan/需求/` 两分类承接。涉及后端仓库的施工契约按仓库边界写入本目录后单独 handoff 给 ExoCore。